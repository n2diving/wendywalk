<!DOCTYPE html lang="en">
<html>
  <head>
    <title>Wendy Walk</title>
    <!-- Bootstrap -->
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
        <div class="jumbotron">
          <h1>Find a Center</h1>
          <%= form_tag mapresults_path, :method => 'get', :class => 'hidden-xs' do %>
              <p>
                <%= text_field_tag :search, params[:search], :class => 'form-control location', placeholder: 'Enter city or state...' %>
                <%= submit_tag "Find a Clinic", :state => nil, :class => 'btn btn-default find' %>
              </p>
            <% end %>
      </div>
      <div class="container-fluid">
      <div class="row">
        <div class="col-md-12">
        </div>
</body>
<script src="http://code.jquery.com/jquery-latest.js"></script>
</html>